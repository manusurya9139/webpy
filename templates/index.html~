$def with (books)

<h1>Books</h1>

<ul>
$for book in books:
    <li>
        <a href="/list/$book.id">$book.name</a> 
        from $datestr(book.posted_on) 
        <a href="/delete/$book.id">Delete</a>
    </li>
</ul>
